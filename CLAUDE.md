# RowLog — 作業規約

慶應義塾高校端艇部の練習記録PWA。卒業研究のデータ収集を兼ねる。
仕様の正本は `docs/REQUIREMENTS.md` と `docs/DESIGN.md`。タスクは `docs/TASKS.md`。

## 絶対ルール

1. **氏名をリポジトリに入れない。**
   GitHub Pages は公開。名簿は Apps Script 経由でスプレッドシートから取る。
   `frontend/` 配下に部員の氏名・ふりがな・保護者情報を書かない。テストデータにも実名を使わない。

2. **APIキーをコードに書かない。** Apps Script のスクリプトプロパティに置く。

3. **欠席・休養のとき、練習時間とRPEに0を入れない。** 空のまま送る。
   0を入れると「0分やった」と「やっていない」が区別できなくなり、解析が壊れる。

4. **RPEの質問文を変えない。** `logic.js` の `RPE_QUESTION` を正本とし、
   画面にはこの定数をそのまま出す。言い方を変えると答えが変わり、比較できなくなる。

5. **写真の読み取りを関門にしない。** 読み取りに失敗しても手入力で提出できること。
   読み取り結果は必ず編集可能。写真そのものは必ず保存する。

6. **依存を増やさない。** フロントはビルド無しの HTML + CSS + JS。
   フレームワーク・バンドラ・npm パッケージを持ち込まない。CDN も使わない。

7. **送信は冪等にする。** `client_id`(UUID) を付け、Apps Script 側で重複を弾く。
   電波の悪い場所での再送で二重計上しないため。

8-2. **カンマ区切りのIDを書く列は「書式なしテキスト」に固定する。**
   スプレッドシートは `"30001,90009"` を桁区切りの数値 `3000190009` と解釈して壊す。
   `TEXT_COLS` に列名を足し、`fixFormats()` で既存シートを直す。実運用で踏んだ。

8. **rate の上限は 60。** 40 で弾かない。実測で 54 が出ている
   （`notes\2026-08-29-ErgShot-Phase0-抽出精度実測.md`）。

9. **コード変更後は必ず `python verify.py` を実行し ALL PASS を維持する。**

9-2. **配信するときは index.html の `?v=` を上げる。**
   スマホのブラウザは js/css を強くキャッシュする。上げ忘れると、直したのに
   古いコードのまま動き続ける（検証中に実際に踏んだ）。`config.js` の VERSION と同じ値にそろえる。

10. **1コミット=1タスク。** 完了ごとに `git add -A && git commit -m "feat(scope): ..."`。

## 凍結事項（変更するならユーザーに確認）

- 回答シートの列（`docs/DESIGN.md` §2）。列の追加は末尾のみ。並べ替え・削除・改名をしない
- `research_id` の体系（A01-A06 / B01-B14 / C01-C24）
- Apps Script の action 名（`bootstrap` / `submit` / `mine` / `extract`）
- 参加状態の4値（実施 / 一部実施 / 欠席 / 休養）と完了度の4値（計画どおり / 短縮 / 中止 / 変更）
- 週間の基本スケジュール（例外は都度予定表で上書き）:
  月=オフ / 火・水・金=陸上練習 / 木=練習日だが種別指定なし / 土・日=乗艇練習
  2026-09-01〜2026-10-31 は seedScheduleOnce（実行後に削除済み）で予定表に反映済み

## 判断済み（蒸し返さない）

- **名簿APIが実名を無認証で返す件は、このままでいく。** 2026-09-03 にユーザーが判断した。
  `?action=bootstrap` を叩けば38人の氏名・学年・クラスが取れ、API URL は
  公開リポジトリの `config.js` に書いてある。入力の摩擦を増やさないことを優先し、
  合い言葉やPINは付けない。卒論の個人情報の節に正直に書く方針。
  下の「ログイン・パスワード・PIN を作らない」はそのまま有効

- **1日1件・RPEは全体で1つ**という集約も凍結のまま。
  同日に陸上と乗艇が重なる日は複数セッションが1つのRPEに潰れるが、
  後から分解はできない。研究の限界として書く

## 踏んだ罠（同じことを繰り返さない）

- **Service Worker の network-first は、それだけでは新しいコードを取れない。**
  `fetch()` はブラウザのHTTPキャッシュを見る。GitHub Pages は HTML に短い
  max-age を付けるので、配信し直した直後でも古い index.html が返り、
  それを Service Worker がさらに保存する。HTML は `cache: 'no-store'` で取る
- **css/js を変えたらバージョンを上げる。** 同じ `?v=` のまま中身だけ変えると、
  ブラウザは古いファイルを掴んだままになる（規約9-2で警告していたのに踏んだ）

## 禁止事項

- 管理画面を作らない（予定表・名簿・メニューの編集はスプレッドシートで直接やる）
- コーチ用ダッシュボード・グラフ・ランキングを作らない
- ログイン・パスワード・PIN を作らない
- 既存の部活マネージャー（clubmanag-4ysnkpfp.manus.space）を書き換えない。読むだけ。RowLog 稼働後に引退させる
- Tailscale・自宅PC常駐を使わない（38人に配れないため）

## 作業環境（2026-09-02 に WSL Ubuntu へ移行）

正本は **WSL Ubuntu の `~/dev/rowlog`**。Windows 側の
`C:\Users\ryu10\Claude-Workspace\projects\rowlog` は移行前の残骸なので触らない。

- リポジトリ: https://github.com/gz7dzc9bkj-design/rowlog （public）
- 公開URL: https://gz7dzc9bkj-design.github.io/rowlog/
- `main` に push すると `.github/workflows/pages.yml` が `frontend/` を Pages に出す
- 検証は `python3 verify.py`（WSL 側で ALL PASS を確認済み）

### 置いてはいけないもの

- `tools/surnames.local.txt` は部員の姓リスト。`.gitignore` 済み。**絶対に push しない**
- 氏名の正本はスプレッドシートの `名簿` シートと
  `Claude-Workspace\box\2026-08-31-研究用ID対応表.xlsx` だけ

### 罠

- **ブラウザ検証は Windows 側の Chrome**。WSL からは screenshot が撮れない
- **Apps Script の編集は必ずスプレッドシートの「拡張機能」メニューから開く**。
  `authuser=1` のURLを直接叩くと「ファイルを開くことができません」になる
- **Apps Script のデプロイダイアログでレンダラが固まる**ことがある。
  固まったらタブを作り直す（コードは保存済みなら失われない）
- **Apps Script の関数ドロップダウンは下までスクロールしてからクリックする**。
  スクロールせずに座標クリックするとエディタ本文を触ってしまう
- **スマホは js/css を強くキャッシュする**。配信時は `?v=` と `config.js` の VERSION を上げる

## ユーザーの流儀

- 「この通りに実装して」と言われたら仕様を忠実に再現する。勝手な再設計・改善をしない
- 改善案があれば実装前に一度だけ提案し、却下されたら従う
- 報告は結果の表 + 次にユーザーがやること、の形式で簡潔に

## 期限

**9月上旬に v1.0 を出して収集を開始する。** 収集は6〜8週間で10月下旬に終わる。
機能を増やすより、9月1日に1件目が入ることを優先する。
